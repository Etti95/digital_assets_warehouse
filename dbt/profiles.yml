crypto_data_warehouse:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'crypto') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'crypto') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'crypto_warehouse') }}"
      schema: analytics
      threads: 4
